'use client';

import Title from "../components/ui/textual/Title";
// import { useUser } from '@/app/contexts/UserContext';

export default function Admin() {

    // const { user } = useUser();

    // console.log(user)

    return (
        <Title>Bonjour admin</Title>
    );
}